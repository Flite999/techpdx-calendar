<!DOCTYPE html>
<html>

<head>
  <title>Search Results - TechPDX Calendar</title>
</head>

<body>
    <div class="mb-8 mt-8 ml-4">
        <h1 class="font-bold text-lg italic"> Search Results </h1>
    </div>
    <div class="ml-4">
        <ul>
          <% @events.each do |event| %>
            <%= render partial: 'event', locals: { event: event } %>
          <% end %>
        </ul>
    </div>
</body>

</html>