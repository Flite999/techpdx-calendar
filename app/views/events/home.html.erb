<!DOCTYPE html>
<html>
<head>
  <title>TechPDX Calendar</title>
</head>
<body>
  <div class="mx-auto max-w-screen-xl px-4 py-4 sm:px-6 sm:py-12 lg:px-8">
    <div class="mb-4">
      <h1 class="font-bold"> Upcoming Events </h1>
    </div>
    <div class="mb-4">
      <div class="mb-2">
        <h2 class="italic font-medium" > Today </h2>
      </div>
      <ul>
        <% @events_today.each do |event| %>
          <%= render partial: 'event_preview', locals: { event: event} %>
        <% end %>
      </ul>
    </div>
    <div class="mb-4">
      <div class="mb-2">
        <h2 class="italic font-medium" > Tomorrow </h2>
      </div>
      <ul>
        <% @events_tomorrow.each do |event| %>
          <%= render partial: 'event_preview', locals: { event: event } %>
        <% end %>
      </ul>
    </div>
    <div class="mb-4">
      <div class="mb-2">
        <h2 class="italic font-medium" > Next Two Weeks </h2>
      </div>
      <ul>
        <% @events_next_two_weeks.each do |event| %>
          <%= render partial: 'event', locals: { event: event } %>
        <% end %>
      </ul>
    </div>
  </div>
</body>
</html>